/*
    test.me - MakeMe file for Http test
 */

Me.load({
    targets: {
        'http-server': {
            type: 'exe',
            depends: [ 'libhttp' ],
            sources: [ 'http-server.c' ],
        },
        test: {
            action: `run('testme --depth ' + me.settings.depth)`,
            platforms: [ 'local' ],
        },

        'clean-test': {
            type: 'clean',                                                                                 
            action: `Cmd.run('testme --clean')`
        },  
    },
})
